import Pricing from '@/components/Pricing';

export const metadata = {
  title: 'Pricing — MoIRA',
  description: 'Simple, transparent pricing for multi-omics research teams of every size.',
};

export default function PricingPage() {
  return <Pricing />;
}
